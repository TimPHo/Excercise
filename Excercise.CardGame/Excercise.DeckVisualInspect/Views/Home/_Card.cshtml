@model Excercise.CardGameDomain.ICard

@{
    var suitName = Model.Suit.ToString().ToLower();
}
<div class="card @(suitName)">
    <div class="name">@Model.Name</div>
    <div class="suit">&@(suitName)s;</div>
</div>

